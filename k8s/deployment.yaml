apiVersion: apps/v1
kind: Deployment
metadata:
  name: "tomom"
  namespace: "agents-dev"
  labels:
    app: "tomom"
    team: "dev"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "tomom"
  template:
    metadata:
      labels:
        app: "tomom"
        team: "dev"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-tomom"
      containers:
        - name: "tomom"
          image: "acrshoppoc.azurecr.io/agents/tomom:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-tomom"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
